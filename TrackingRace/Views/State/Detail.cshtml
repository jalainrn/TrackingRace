@model TrackingRace.ViewModels.StateViewModel

@Html.Partial("_Message")

<h2>@ViewBag.Title</h2>

<div class="row">
    <div class="col-sm-6 col-xs-10">
        @using (Html.BeginForm("State Detail"))
        {

            @Html.HiddenFor(m => m.Id)
            <div class="form-group col-md-10">
                @Html.LabelFor(m => m.Name)
                @Html.DisplayFor(m => m.Name)
            </div>
            <div class="form-group col-md-10">
                @Html.LabelFor(m => m.Acronym)
                @Html.DisplayFor(m => m.Acronym)
            </div>

            <div class="form-group col-md-10 mt-4">
                @Html.ActionLink("Go Back", "Index", null, new { @class = "btn btn-secondary" })
            </div>
        }
    </div>
</div>







