@model TrackingRace.ViewModels.StateViewModel

@Html.Partial("_Message")

@{
    var isEditMode = Model.Id != null;
    ViewBag.Title = isEditMode ? "Edit" : "Add State";
}

<h2>@ViewBag.Title</h2>


<div class="row">
    <div class="col-sm-6 col-xs-10">
        @using (Html.BeginForm(isEditMode ? "Edit" : "Add", "State"))
        {
            @Html.ValidationSummary("The following errors were found:", new { @class = "alert alert-dismissible alert-danger"})

            @Html.HiddenFor(m => m.Id)
            <div class="form-group col-md-10">
                @Html.LabelFor(m => m.Name)
                @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
            </div>
            <div class="form-group col-md-2">
                @Html.LabelFor(m => m.Acronym)
                @Html.TextBoxFor(m => m.Acronym, new { @class = "form-control" })
            </div>

            <div class="form-group col-md-10 mt-4">
                <button type="submit" class="btn btn-primary">@(isEditMode ? "Save" : "Add")</button>
                @Html.ActionLink("Cancel", "Index", null, new { @class = "btn btn-secondary" })
            </div>
        }
    </div>
</div>